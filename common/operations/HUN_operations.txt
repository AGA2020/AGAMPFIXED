rescue_bela_kun_operation = { ### 1919 Path
	icon = GFX_operations_rescue_captured_operative
	map_icon = GFX_operations_rescue_captured_operative_map
	name = rescue_bela_kun_operation
	desc = rescue_bela_kun_operation_desc
	days = 60

	priority = 100

	allowed = {
		original_tag = HUN
	}
	available = { 
		network_strength = {
			target = SOV
			state = 219 # Moscow
			value > 25
		}
	}
	visible = {
		has_completed_focus = HUN_rescue_bela_kun
		NOT = { has_global_flag = flag_kun_dead }
		FROM = {
			controls_state = 219
		}
		NOT = {
			SOV = { has_completed_focus = SOV_the_bloc_of_rights_and_trotskyites }
		}
	}

	operatives = 1

	# Integer [0-100] - Every operation has a hard requirement of network strength in the target country
	network_strength = 40

	equipment = {
		infantry_equipment = 50
		support_equipment = 50
	}

	on_start = {  # SCOPE_OPERATION
		
	}

	outcome_modifiers = { operation_outcome }
	cost_modifiers = { operation_cost }
	risk_modifiers = { operation_risk }

	outcome_potential = {
		custom_effect_tooltip = HUN_rescue_bela_kun_outcome_tt
	}
	risk_chance = 0.1
	experience = 1
	# outcome is an effect that runs when the operation has completed
	outcome_execute = { # SCOPE_OPERATION
		hidden_effect = {
			ROOT = { set_country_flag = HUN_bela_kun_rescued_flag } #For AI Strat
		}
		ROOT = { news_event = { id = LH_news.27 } } #SUCCESS - Béla Kun was rescued
		SOV = { 
			add_opinion_modifier = { target = ROOT modifier = HUN_SOV_rescued_kun } 
			if = {
				limit = { has_opinion_modifier = HUN_SOV_tried_rescued_kun } 
				remove_opinion_modifier = { target = HUN modifier = HUN_SOV_tried_rescued_kun } 
			}
		}
	}

	outcome_extra_chance = 0.2

	outcome_extra_execute = {
		hidden_effect = {
			ROOT = { set_country_flag = HUN_bela_kun_failed_rescue_flag } #For AI Strat
		}
		ROOT = { news_event = { id = LH_news.28 } } #FAILURE - Béla Kun died
		SOV = { add_opinion_modifier = { target = HUN modifier = HUN_SOV_tried_rescued_kun } }
	}

	# This is where all the separate phases are defined
	# Each phase for an operation may have a single actual phase assigned to it, which is determined here
	phases = { #infiltration
		infiltration_border = { base = 25 }
		infiltration_submarine = { base = 25 }
		infiltration_paradrop = { 
			base = 25 
			modifier = {
				ROOT = { 
					has_equipment = {
						transport_plane_equipment < 1
					}
				}
				factor = 0
			}
		}
		infiltration_diplomatic = {
			base = 25
			modifier = {
				factor = 20
				ROOT = { has_war = no }
				FROM = {
					has_war = no
				}
			}
			modifier = {
				FROM = {
					has_war_with = ROOT
				}
				factor = 0
			}
		}
	}
	phases = { 
		free_operative_free_in_transit = { base = 33 }
		free_operative_liberate_camp = { base = 33 }
		free_operative_smuggle_in_escape_gear = { base = 33 }
	}
	phases = { 
		exfiltration_air_pickup = { 
			base = 15 
			modifier = {
				ROOT = { 
					has_equipment = {
						transport_plane_equipment < 1
					}
				}
				factor = 0
			}
		}
		exfiltration_border = { base = 30 }
		exfiltration_go_to_ground = { base = 30 }
		exfiltration_submarine_pickup = { base = 25 }
	}
}
